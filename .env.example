# ─── Base de datos PostgreSQL ───────────────────────────────────────────────
# host.docker.internal apunta a localhost del host desde dentro del contenedor
DATABASE_URL="postgresql://USUARIO:CONTRASEÑA@host.docker.internal:5432/NOMBRE_DB?schema=public"

# ─── Webhook GitHub ──────────────────────────────────────────────────────────
# Poné el mismo valor en GitHub → Settings → Webhooks → Secret
WEBHOOK_SECRET=cambia_esto_por_un_secreto_aleatorio
